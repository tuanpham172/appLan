name: Build Android APK
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Hard Clean and Build
        run: |
          APP_PATH=$(find . -name "package.json" -not -path "*/node_modules/*" -printf '%h\n' | head -n 1)
          cd "$APP_PATH"
          
          # Lệnh xóa cực mạnh để dọn dẹp node_modules lỡ upload từ Windows
          rm -rf node_modules package-lock.json dist android
          
          # Cài đặt lại từ đầu bản chuẩn cho Linux
          npm install --force
          
          # Build web
          npm run build --if-present
          
          # Thiết lập Capacitor
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init "AppLan" "com.tuanpham.applan" --web-dir dist || true
          npx cap add android || true
          npx cap copy android
          
          # Build APK
          if [ -d "android" ]; then
            cd android
            chmod +x gradlew
            ./gradlew assembleDebug
          else
            echo "Thư mục Android không tồn tại!" && exit 1
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: "**/app/build/outputs/apk/debug/*.apk"
